# 假回锁 6.2.216

本次版本重点解决系统全量更新后，模块对另一槽位执行 `vendor_boot` 或 `super` 修补时“处理成功但未真正写入”的问题，并精简刷写日志。

- 写入前自动解除目标分区只读状态，并支持重试。
- 写入前检查镜像与分区容量，避免越界或不完整写入。
- 写入后刷新块设备缓存并回读校验，确认数据真正落盘。
- 修正 ABL 已成功修改 `STRB` 却仍提示失败的误报。
- WebUI 成功日志只显示关键完成结果；失败时保留失败步骤和直接原因。
- 模块版本改为统一管理，并由构建流程检查逐次递增。
