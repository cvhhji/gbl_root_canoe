# 更新日志

## 6.2.217

- 恢复 ABL 提取、修补、设置可写和分区复制的完整刷写日志。
- 为 `vendor_boot` 与 `super` 修补补充开始和完成日志，底层冗长输出仍保持隐藏。
- WebUI 增加中文与英文即时切换按钮，选择结果会同步保存到模块配置。
- 安装时优先沿用已有语言，没有历史设置时根据系统语言选择默认值，并允许在十秒内用音量键覆盖。
- 修复重复追加模块名称和描述的问题，语言切换改为更新现有元数据。

## 6.2.216

- WebUI 刷写日志改为摘要模式，成功时仅记录完成结果，不再展示解包、指令扫描和底层复制输出。
- 操作失败时保留失败步骤和一条经过截断的直接原因，便于排查且避免日志刷屏。

## 6.2.215

- 修正 ABL 修补器将已经成功修改的 `STRB` 错误报告为失败的问题，避免日志出现互相矛盾的警告。

## 6.2.214

本次版本重点修复系统更新后向另一槽位写入 `super` 或 `vendor_boot` 失败的问题，并整理了模块构建与版本管理流程。

- 增强非活动槽位分区写入：写入前主动解除只读状态，并在失败时给出明确原因。
- 增加镜像与目标分区大小检查，避免错误镜像或异常分区造成不完整写入。
- 写入后刷新块设备缓存并回读校验，降低日志显示修补成功但实际未落盘的风险。
- 修正模块安装脚本中的路径引用和临时文件清理，提升带空格路径及不同安装环境下的稳定性。
- 将版本号集中到根目录 `VERSION`，构建时自动生成模块元数据，避免模块版本长期漏改。
- 构建流程要求版本号逐次递增，并校验最终模块中的版本信息。
- 统一常用源码与脚本的换行规则，清理本仓库自定义代码中的说明性注释。
